<%= form_with(model: patient, local: true, class: "space-y-6", html: { 'aria-describedby': (patient.errors.any? ? 'form-errors' : nil) }) do |form| %>
  <% if patient.errors.any? %>
    <div id="form-errors" role="alert" class="bg-accent-danger/10 border border-accent-danger text-accent-danger px-4 py-3 rounded">
      <h2 class="font-bold mb-2"><%= pluralize(patient.errors.count, "件") %>のエラーがあります：</h2>
      <ul class="list-disc list-inside">
        <% patient.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if patient.questionnaire.present? %>
    <%# 問診票が存在する場合は基本情報を表示のみ %>
    <div class="bg-accent-primary/10 border border-accent-primary rounded p-4 mb-4">
      <p class="text-sm text-ink-dark font-medium mb-2">基本情報は問診票から管理されています</p>
      <p class="text-sm text-ink-dark">基本情報（氏名・生年月日・性別・電話番号）を変更する場合は、問診票を編集してください。</p>
      <%= link_to "問診票を編集", edit_patient_questionnaire_path(patient), class: "inline-block mt-2 text-ink-dark hover:text-accent-primary transition-colors underline transition-colors" %>
    </div>

    <div class="bg-greige-100 rounded p-4 space-y-4">
      <div>
        <dt class="text-sm font-medium text-gray-500">氏名</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= h(patient.questionnaire.full_name) %></dd>
      </div>

      <% if patient.questionnaire.birth_date.present? %>
        <div>
          <dt class="text-sm font-medium text-gray-500">生年月日</dt>
          <dd class="mt-1 text-lg text-gray-900">
            <%= h(begin
                  Date.parse(patient.questionnaire.birth_date.to_s).strftime('%Y年%m月%d日')
                rescue
                  patient.questionnaire.birth_date
                end) %>
          </dd>
        </div>
      <% end %>

      <% if patient.questionnaire.gender.present? %>
        <div>
          <dt class="text-sm font-medium text-gray-500">性別</dt>
          <dd class="mt-1 text-lg text-gray-900">
            <%= h(case patient.questionnaire.gender
                when 'male' then '男性'
                when 'female' then '女性'
                when 'other' then 'その他'
                when 'prefer_not_to_say' then '回答しない'
                else patient.questionnaire.gender
                end) %>
          </dd>
        </div>
      <% end %>

      <div>
        <dt class="text-sm font-medium text-gray-500">電話番号</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= h(patient.questionnaire.phone) %></dd>
      </div>
    </div>
  <% else %>
    <%# 問診票が存在しない場合は基本情報を編集可能 %>
    <div>
      <%= form.label :name, "氏名", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= form.text_field :name, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", placeholder: "例：山田太郎", required: true %>
    </div>

    <div>
      <%= form.label :date_of_birth, "生年月日", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= form.date_field :date_of_birth, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary" %>
    </div>

    <div>
      <%= form.label :gender, "性別", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= form.select :gender,
          Patient.genders.keys.map { |key| [t("activerecord.attributes.patient.genders.#{key}"), key] },
          { prompt: "選択してください" },
          { class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", data: { controller: 'tom-select' } } %>
    </div>

    <div>
      <%= form.label :phone, "電話番号", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= form.text_field :phone, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", placeholder: "例：090-1234-5678" %>
    </div>
  <% end %>

  <div>
    <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.email_field :email, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", placeholder: "例：patient@example.com" %>
  </div>

  <div>
    <%= form.label :address, "住所", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.text_area :address, rows: 3, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", placeholder: "例：東京都渋谷区1-1-1" %>
  </div>

  <div>
    <%= form.label :emergency_contact, "緊急連絡先", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.text_field :emergency_contact, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary", placeholder: "例：03-1234-5678" %>
  </div>

  <div class="flex justify-end space-x-3">
    <%= cancel_button "キャンセル", patients_path %>
    <%= form.submit "保存", class: "bg-accent-primary hover:bg-accent-primary/90 text-white font-bold py-2 px-4 rounded transition-colors" %>
  </div>
<% end %>
