<div class="container mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-900">お問い合わせ詳細</h1>
    <%= link_to "一覧に戻る", admin_inquiries_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <div class="bg-white shadow-md rounded p-6 mb-6">
    <dl class="grid grid-cols-1 gap-6">
      <div>
        <dt class="text-sm font-medium text-gray-500">件名</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= @inquiry.subject %></dd>
      </div>

      <div>
        <dt class="text-sm font-medium text-gray-500">ユーザー情報</dt>
        <dd class="mt-1 text-lg text-gray-900">
          <%= @inquiry.user.name %> (<%= @inquiry.user.email %>)
        </dd>
      </div>

      <div>
        <dt class="text-sm font-medium text-gray-500">カテゴリ</dt>
        <dd class="mt-1">
          <span class="px-2 py-1 text-sm font-medium rounded-full bg-gray-100 text-gray-800">
            <%= @inquiry.category_i18n %>
          </span>
        </dd>
      </div>

      <div>
        <dt class="text-sm font-medium text-gray-500">作成日時</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= l(@inquiry.created_at, format: :long) %></dd>
      </div>

      <div>
        <dt class="text-sm font-medium text-gray-500">最終更新日時</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= l(@inquiry.updated_at, format: :long) %></dd>
      </div>
    </dl>

    <div class="mt-6 pt-6 border-t border-gray-200">
      <h3 class="text-lg font-semibold text-gray-900 mb-3">ステータス変更</h3>
      <%= form_with model: @inquiry, url: admin_inquiry_path(@inquiry), method: :patch, local: true, class: 'flex gap-3 items-end' do |f| %>
        <div class="flex-1">
          <%= f.label :status, 'ステータス', class: 'block text-sm font-medium text-gray-700 mb-2' %>
          <%= f.select :status,
            options_for_select([
              ['未対応', 'open'],
              ['対応中', 'in_progress'],
              ['対応完了', 'closed']
            ], @inquiry.status),
            {},
            class: 'block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500' %>
        </div>
        <%= f.submit '更新', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
      <% end %>
    </div>
  </div>

  <div class="bg-white shadow-md rounded p-6 mb-6">
    <h2 class="text-xl font-bold text-gray-900 mb-4">メッセージ履歴</h2>
    <div class="space-y-4">
      <% @inquiry_messages.each do |message| %>
        <div class="<%= message.user.admin? ? 'bg-blue-50 border-l-4 border-blue-500' : 'bg-gray-50 border-l-4 border-gray-300' %> p-4 rounded">
          <div class="flex justify-between items-center mb-2">
            <div class="flex items-center gap-2">
              <span class="font-semibold text-gray-900"><%= message.user.name %></span>
              <% if message.user.admin? %>
                <span class="px-2 py-1 text-xs font-medium rounded-full bg-blue-100 text-blue-800">管理者</span>
              <% end %>
            </div>
            <span class="text-sm text-gray-500">
              <%= l(message.created_at, format: :short) %>
            </span>
          </div>
          <div class="text-gray-700 whitespace-pre-wrap"><%= message.body %></div>
        </div>
      <% end %>
    </div>
  </div>

  <% unless @inquiry.closed? %>
    <div class="bg-white shadow-md rounded p-6">
      <h3 class="text-xl font-bold text-gray-900 mb-4">管理者として返信する</h3>
      <%= form_with model: [@inquiry, @inquiry_message], url: admin_inquiry_inquiry_messages_path(@inquiry), local: true, class: 'space-y-4' do |f| %>
        <% if @inquiry_message.errors.any? %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
            <ul class="list-disc list-inside text-sm">
              <% @inquiry_message.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div>
          <%= f.label :body, '本文', class: 'block text-sm font-medium text-gray-700 mb-2' %>
          <%= f.text_area :body, rows: 5, class: 'mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500', placeholder: '返信内容を入力してください' %>
        </div>

        <div class="flex justify-end">
          <%= f.submit '返信', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded">
      <p>このお問い合わせは閉じられています。</p>
    </div>
  <% end %>
</div>
