<div class="container mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-900">お問い合わせ管理</h1>
  </div>

  <div class="bg-white shadow-md rounded p-4 mb-6">
    <h3 class="font-semibold text-gray-900 mb-3">フィルター</h3>
    <%= form_with url: admin_inquiries_path, method: :get, local: true, class: 'flex gap-3 flex-wrap items-end' do |f| %>
      <div>
        <%= f.label :status, 'ステータス', class: 'block text-sm font-medium text-gray-700 mb-1' %>
        <%= f.select :status,
          options_for_select([
            ['全て', ''],
            ['未対応', 'open'],
            ['対応中', 'in_progress'],
            ['対応完了', 'closed']
          ], params[:status]),
          {},
          class: 'rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500' %>
      </div>
      <div>
        <%= f.label :category, 'カテゴリ', class: 'block text-sm font-medium text-gray-700 mb-1' %>
        <%= f.select :category,
          options_for_select([
            ['全て', '']
          ] + Inquiry.categories.keys.map { |key| [Inquiry.human_attribute_name("categories.#{key}"), key] },
          params[:category]),
          {},
          class: 'rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500' %>
      </div>
      <%= f.submit 'フィルター', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
    <% end %>
  </div>

  <% if @inquiries.any? %>
    <div class="bg-white shadow-md rounded">
      <table class="min-w-full">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-3 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider w-16">未読</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">件名</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ユーザー</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">カテゴリ</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ステータス</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">更新日時</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @inquiries.each do |inquiry| %>
            <tr>
              <td class="px-3 py-4 text-center">
                <%= unread_indicator_for(inquiry, current_user) %>
              </td>
              <td class="px-6 py-4">
                <%= link_to inquiry.subject, admin_inquiry_path(inquiry), class: "text-blue-600 hover:text-blue-900" %>
              </td>
              <td class="px-6 py-4 text-sm text-gray-900">
                <%= inquiry.user.name %>
              </td>
              <td class="px-6 py-4">
                <span class="px-2 py-1 text-xs font-medium rounded-full bg-gray-100 text-gray-800">
                  <%= inquiry.category_i18n %>
                </span>
              </td>
              <td class="px-6 py-4">
                <span class="px-2 py-1 text-xs font-medium rounded-full <%= inquiry.open? ? 'bg-blue-100 text-blue-800' : inquiry.in_progress? ? 'bg-yellow-100 text-yellow-800' : 'bg-gray-100 text-gray-800' %>">
                  <%= inquiry.status_i18n %>
                </span>
              </td>
              <td class="px-6 py-4 text-sm text-gray-500">
                <%= l(inquiry.updated_at, format: :short) %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">
                <%= link_to "表示", admin_inquiry_path(inquiry), class: "text-blue-600 hover:text-blue-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="mt-6 flex justify-center">
      <%= paginate @inquiries %>
    </div>
  <% else %>
    <div class="bg-white shadow-md rounded p-8 text-center">
      <p class="text-gray-600 mb-2">
        <%= params[:status].present? || params[:category].present? ? '該当するお問い合わせはありません。' : 'まだお問い合わせはありません。' %>
      </p>
      <p class="text-gray-500 text-sm">
        <%= params[:status].present? || params[:category].present? ? 'フィルター条件を変更してください。' : 'ユーザーからのお問い合わせがここに表示されます。' %>
      </p>
    </div>
  <% end %>
</div>
