<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- ヘッダー -->
  <div class="mb-8 flex justify-between items-center">
    <div>
      <h1 class="text-3xl font-bold text-greige-900">お知らせ管理</h1>
      <p class="mt-2 text-sm text-greige-600">システムお知らせの作成と管理</p>
    </div>
    <%= new_button "新規作成", new_admin_announcement_path %>
  </div>

  <!-- お知らせ一覧 -->
  <div class="bg-white shadow-sm rounded-lg overflow-hidden">
    <% if @announcements.any? %>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-greige-200" role="table" aria-label="お知らせ一覧">
          <caption class="sr-only">お知らせ管理テーブル</caption>
          <thead class="bg-greige-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-greige-500 uppercase tracking-wider">
                タイトル
              </th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-greige-500 uppercase tracking-wider">
                重要度
              </th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-greige-500 uppercase tracking-wider">
                ステータス
              </th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-greige-500 uppercase tracking-wider">
                公開日時
              </th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-greige-500 uppercase tracking-wider">
                作成者
              </th>
              <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-greige-500 uppercase tracking-wider">
                操作
              </th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-greige-200">
            <% @announcements.each do |announcement| %>
              <tr class="hover:bg-greige-50">
                <td class="px-6 py-4">
                  <%= link_to announcement.title, admin_announcement_path(announcement), class: "text-sm text-accent-primary hover:text-accent-primary/90 transition-colors font-medium" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= case announcement.severity
                    when 'info' then 'bg-blue-100 text-blue-800'
                    when 'warning' then 'bg-accent-warning/10 text-yellow-800'
                    when 'critical' then 'bg-accent-danger/10 text-red-800'
                    end %>">
                    <%= announcement.severity_i18n %>
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= case announcement.status
                    when 'draft' then 'bg-greige-100 text-greige-800'
                    when 'published' then 'bg-accent-success/10 text-accent-success/90'
                    when 'archived' then 'bg-greige-100 text-greige-600'
                    end %>">
                    <%= announcement.status_i18n %>
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-greige-500">
                  <%= announcement.published_at&.strftime('%Y/%m/%d %H:%M') || '-' %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-greige-900">
                  <%= announcement.author.email %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                  <div class="flex gap-2 justify-end">
                    <%= edit_button "編集", edit_admin_announcement_path(announcement) %>
                    <% if announcement.draft? %>
                      <%= button_to "公開", publish_admin_announcement_path(announcement), method: :patch, class: "inline-flex items-center px-3 py-1 border border-transparent rounded-md text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" %>
                    <% end %>
                    <% if announcement.published? %>
                      <%= button_to "アーカイブ", archive_admin_announcement_path(announcement), method: :patch, class: "inline-flex items-center px-3 py-1 border border-greige-300 rounded-md text-sm font-medium text-greige-700 bg-white hover:bg-greige-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-accent-primary transition-colors" %>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <!-- ページネーション -->
      <div class="px-6 py-4 bg-greige-50 border-t border-greige-200">
        <%= paginate @announcements %>
      </div>
    <% else %>
      <div class="px-6 py-12 text-center">
        <p class="text-sm text-greige-500">お知らせがありません</p>
      </div>
    <% end %>
  </div>
</div>
