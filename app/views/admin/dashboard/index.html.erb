<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- ヘッダー -->
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-greige-900">管理者ダッシュボード</h1>
    <p class="mt-2 text-sm text-greige-600">システム統計とユーザー管理</p>
  </div>

  <!-- システム統計 -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
    <!-- 総ユーザー数 -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border-l-4 border-accent-primary">
      <div class="p-6">
        <p class="text-sm font-medium text-greige-600">総ユーザー数</p>
        <p class="mt-2 text-4xl font-bold text-accent-primary"><%= @total_users %></p>
        <%= link_to "ユーザー管理へ", admin_users_path, class: "mt-4 inline-flex items-center text-sm font-medium text-accent-primary hover:text-accent-primary/90 transition-colors" %>
      </div>
    </div>

    <!-- 総施術記録数 -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border-l-4 border-accent-success">
      <div class="p-6">
        <p class="text-sm font-medium text-greige-600">総施術記録数</p>
        <p class="mt-2 text-4xl font-bold text-accent-success"><%= @total_medical_records %></p>
      </div>
    </div>

    <!-- アクティブなお知らせ -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg border-l-4 border-accent-warning">
      <div class="p-6">
        <p class="text-sm font-medium text-greige-600">公開中のお知らせ</p>
        <p class="mt-2 text-4xl font-bold text-accent-warning"><%= @active_announcements %></p>
        <%= link_to "お知らせ管理へ", admin_announcements_path, class: "mt-4 inline-flex items-center text-sm font-medium text-accent-warning hover:text-accent-warning transition-colors" %>
      </div>
    </div>
  </div>

  <!-- クイックリンク -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- 最近登録されたユーザー -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg">
      <div class="p-6">
        <h2 class="text-lg font-semibold text-greige-900 mb-4">最近登録されたユーザー</h2>

        <% if @recent_users.any? %>
          <div class="overflow-hidden">
            <table class="min-w-full divide-y divide-greige-200">
              <tbody class="divide-y divide-greige-200">
                <% @recent_users.each do |user| %>
                  <tr class="hover:bg-greige-50">
                    <td class="py-3">
                      <%= link_to user.email, admin_user_path(user), class: "text-sm text-accent-primary hover:text-accent-primary/90 transition-colors" %>
                    </td>
                    <td class="py-3 text-right">
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= user.admin? ? 'bg-accent-danger/10 text-accent-danger' : 'bg-accent-primary/10 text-accent-primary' %>">
                        <%= user.admin? ? '管理者' : 'ユーザー' %>
                      </span>
                    </td>
                    <td class="py-3 text-right text-sm text-greige-500">
                      <%= user.created_at.strftime('%Y/%m/%d') %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        <% else %>
          <p class="text-sm text-greige-500">ユーザーがいません</p>
        <% end %>
      </div>
    </div>

    <!-- 最近公開されたお知らせ -->
    <div class="bg-white overflow-hidden shadow-sm rounded-lg">
      <div class="p-6">
        <h2 class="text-lg font-semibold text-greige-900 mb-4">最近公開されたお知らせ</h2>

        <% if @recent_announcements.any? %>
          <div class="space-y-3">
            <% @recent_announcements.each do |announcement| %>
              <div class="border-l-4 <%= announcement.severity == 'critical' ? 'border-accent-danger' : announcement.severity == 'warning' ? 'border-accent-warning' : 'border-accent-primary' %> pl-3">
                <%= link_to announcement.title, admin_announcement_path(announcement), class: "text-sm text-accent-primary hover:text-accent-primary/90 transition-colors block" %>
                <p class="text-xs text-greige-500 mt-1">
                  <%= announcement.published_at&.strftime('%Y/%m/%d %H:%M') %>
                </p>
              </div>
            <% end %>
          </div>
        <% else %>
          <p class="text-sm text-greige-500">お知らせがありません</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
