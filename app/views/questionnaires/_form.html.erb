<%= form_with(model: questionnaire, url: patient_questionnaire_path(patient), local: true, class: "space-y-8", data: { controller: "questionnaire", action: "submit->questionnaire#handleSubmit" }) do |form| %>
  <% if questionnaire.errors.any? %>
    <div class="bg-accent-danger/10 border border-accent-danger text-accent-danger px-4 py-3 rounded">
      <h2 class="font-bold mb-2"><%= pluralize(questionnaire.errors.count, "件") %>のエラーがあります：</h2>
      <ul class="list-disc list-inside">
        <% questionnaire.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%# セクション1: 基本情報 %>
  <div class="bg-white border-2 border-greige-200 rounded-lg p-6">
    <h2 class="text-2xl font-bold text-greige-900 mb-6 pb-3 border-b-2 border-blue-500">基本情報</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <%= form.label :full_name, "氏名（必須）", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :full_name, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "山田 太郎", required: true %>
      </div>

      <div>
        <%= form.label :full_name_kana, "フリガナ（必須）", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :full_name_kana, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "ヤマダ タロウ", required: true %>
      </div>

      <div>
        <%= form.label :birth_date, "生年月日（必須）", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.date_field :birth_date, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", required: true %>
      </div>

      <div>
        <%= form.label :gender, "性別（必須）", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <div class="mt-2 space-y-2">
          <label class="inline-flex items-center mr-6">
            <%= form.radio_button :gender, "male", class: "form-radio h-5 w-5 text-accent-primary" %>
            <span class="ml-2 text-lg">男性</span>
          </label>
          <label class="inline-flex items-center mr-6">
            <%= form.radio_button :gender, "female", class: "form-radio h-5 w-5 text-accent-primary" %>
            <span class="ml-2 text-lg">女性</span>
          </label>
          <label class="inline-flex items-center mr-6">
            <%= form.radio_button :gender, "other", class: "form-radio h-5 w-5 text-accent-primary" %>
            <span class="ml-2 text-lg">その他</span>
          </label>
          <label class="inline-flex items-center">
            <%= form.radio_button :gender, "prefer_not_to_say", class: "form-radio h-5 w-5 text-accent-primary" %>
            <span class="ml-2 text-lg">回答しない</span>
          </label>
        </div>
      </div>

      <div>
        <%= form.label :phone, "電話番号（必須）", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.telephone_field :phone, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "090-1234-5678", required: true %>
      </div>

      <div>
        <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :email, type: :email, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "example@email.com" %>
      </div>

      <div>
        <%= form.label :postal_code, "郵便番号", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :postal_code, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "123-4567" %>
      </div>

      <div class="md:col-span-2">
        <%= form.label :address, "住所", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :address, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "東京都渋谷区..." %>
      </div>

      <div class="md:col-span-2">
        <%= form.label :emergency_contact, "緊急連絡先", class: "block text-sm font-medium text-greige-700 mb-2" %>
        <%= form.text_field :emergency_contact, class: "mt-1 block w-full rounded-md border-greige-300 shadow-sm focus:border-accent-primary focus:ring-accent-primary text-lg p-3", placeholder: "本人以外の連絡先（氏名・続柄・電話番号）" %>
      </div>
    </div>
  </div>

  <%# セクション2&3: UI切り替え（フィーチャーフラグ） %>
  <% if @use_checkbox_ui %>
    <%= render 'form_checkbox', form: form %>
  <% else %>
    <%= render 'form_textarea', form: form %>
  <% end %>

  <!-- 看護師確認セクション -->
  <div class="mt-8 bg-accent-warning/10 border border-accent-warning/30 rounded-lg p-6">
    <h3 class="text-lg font-semibold text-greige-900 mb-4 flex items-center">
      <svg class="h-5 w-5 mr-2 text-accent-warning" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
      </svg>
      看護師確認
    </h3>
    <div class="flex items-center">
      <%= form.check_box :nurse_confirmed, class: "h-5 w-5 text-accent-primary border-gray-300 rounded focus:ring-accent-primary" %>
      <%= form.label :nurse_confirmed, "問診内容を確認しました", class: "ml-3 text-sm font-medium text-greige-900" %>
    </div>
    <p class="mt-2 text-xs text-greige-500">※ チェックを入れると確認日時が記録されます</p>
  </div>

  <div class="flex justify-end space-x-3 pt-4">
    <%= cancel_button "キャンセル", patient_path(patient) %>
    <%= form.submit "保存", class: "bg-accent-primary hover:bg-accent-primary/90 text-white font-bold py-3 px-6 rounded text-lg transition-colors" %>
  </div>
<% end %>

<script>
  // 注: Stimulusフレームワークのロードに失敗した場合のフォールバック
  // questionnaire_controller.jsと同様の処理を実装
  (function() {
    const form = document.querySelector('form[data-controller="questionnaire"]');
    if (!form) return;

    form.addEventListener('submit', function(event) {
      event.preventDefault(); // フォーム送信を一時停止

      // ヘルパー関数: 隠しフィールドに安全に値を設定
      const setHiddenField = (fieldId, value) => {
        const field = document.getElementById(fieldId);
        if (field) {
          const jsonValue = typeof value === 'string' ? value : JSON.stringify(value);
          field.value = jsonValue;
        }
      };

      // 既往歴・治療中の病気
      const medicalConditions = Array.from(form.querySelectorAll('input[name="medical_conditions[]"]:checked')).map(cb => cb.value);
      const otherConditionDetail = document.querySelector('input[name="other_medical_condition"]');
      if (otherConditionDetail && otherConditionDetail.value.trim()) {
        medicalConditions.push(`その他: ${otherConditionDetail.value.trim()}`);
      }
      setHiddenField('medical_conditions_json', medicalConditions);

      // アレルギー
      const allergies = Array.from(form.querySelectorAll('input[name="allergies[]"]:checked')).map(cb => cb.value);
      const drugAllergyDetail = document.querySelector('input[name="drug_allergy_detail"]');
      if (drugAllergyDetail && drugAllergyDetail.value.trim()) {
        allergies.push(`薬物: ${drugAllergyDetail.value.trim()}`);
      }
      const foodAllergyDetail = document.querySelector('input[name="food_allergy_detail"]');
      if (foodAllergyDetail && foodAllergyDetail.value.trim()) {
        allergies.push(`食物: ${foodAllergyDetail.value.trim()}`);
      }
      const otherAllergyDetail = document.querySelector('input[name="other_allergy"]');
      if (otherAllergyDetail && otherAllergyDetail.value.trim()) {
        allergies.push(`その他: ${otherAllergyDetail.value.trim()}`);
      }
      setHiddenField('allergies_json', allergies);

      // 服薬中の薬
      const medications = Array.from(form.querySelectorAll('input[name="medications[]"]:checked')).map(cb => cb.value);
      const supplementDetail = document.querySelector('input[name="supplement_detail"]');
      if (supplementDetail && supplementDetail.value.trim()) {
        medications.push(`サプリメント: ${supplementDetail.value.trim()}`);
      }
      const otherMedicationDetail = document.querySelector('input[name="other_medication"]');
      if (otherMedicationDetail && otherMedicationDetail.value.trim()) {
        medications.push(`その他: ${otherMedicationDetail.value.trim()}`);
      }
      setHiddenField('current_medications_json', medications);

      // 手術歴
      const surgeryDetail = document.getElementById('surgery_details');
      const surgeries = surgeryDetail && surgeryDetail.value.trim() ? [surgeryDetail.value.trim()] : [];
      setHiddenField('past_surgeries_json', surgeries);

      // 妊娠・授乳状況
      const pregnancy = Array.from(form.querySelectorAll('input[name="pregnancy_status[]"]:checked')).map(cb => cb.value);
      setHiddenField('pregnancy_info_json', pregnancy);

      // 希望施術部位
      const desiredTreatments = Array.from(form.querySelectorAll('input[name="desired_treatments[]"]:checked')).map(cb => cb.value);
      const otherTreatmentDetail = document.querySelector('input[name="other_treatment"]');
      if (otherTreatmentDetail && otherTreatmentDetail.value.trim()) {
        desiredTreatments.push(`その他: ${otherTreatmentDetail.value.trim()}`);
      }
      setHiddenField('desired_treatments_json', desiredTreatments);

      // 過去のアートメイク経験
      const pastTreatments = Array.from(form.querySelectorAll('input[name="past_treatment_parts[]"]:checked')).map(cb => cb.value);

      // 施術時期
      const pastTreatmentDate = document.querySelector('input[name="past_treatment_date"]');
      if (pastTreatmentDate && pastTreatmentDate.value.trim()) {
        pastTreatments.push(`施術時期: ${pastTreatmentDate.value.trim()}`);
      }

      // 施術場所
      const pastTreatmentLocation = document.querySelector('input[name="past_treatment_location"]');
      if (pastTreatmentLocation && pastTreatmentLocation.value.trim()) {
        pastTreatments.push(`施術場所: ${pastTreatmentLocation.value.trim()}`);
      }

      // トラブルの有無
      const hadTrouble = document.querySelector('input[name="had_trouble"]:checked');
      const troubleDetail = document.querySelector('input[name="trouble_detail"]');
      if (hadTrouble && hadTrouble.value === 'true' && troubleDetail && troubleDetail.value.trim()) {
        pastTreatments.push(`トラブル: ${troubleDetail.value.trim()}`);
      } else if (hadTrouble && hadTrouble.value === 'false') {
        pastTreatments.push('トラブル: なし');
      }

      setHiddenField('past_treatments_json', pastTreatments);

      // 肌の状態
      const skinConditions = Array.from(form.querySelectorAll('input[name="skin_conditions[]"]:checked')).map(cb => cb.value);
      setHiddenField('skin_conditions_json', skinConditions);

      // すべてのJavaScript処理が完了したら、フォームを送信
      event.target.submit();
    });
  })();
</script>
